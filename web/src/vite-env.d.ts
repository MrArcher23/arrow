/// <reference types="svelte" />
/// <reference types="vite/client" />

// Injected by Vite's `define` (see vite.config.ts) — the app version read from
// src-tauri/tauri.conf.json at build time.
declare const __APP_VERSION__: string
